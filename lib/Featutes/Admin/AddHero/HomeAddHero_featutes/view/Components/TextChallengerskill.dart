import 'package:flutter/material.dart';

class TextChallengerskill extends StatelessWidget {
  const TextChallengerskill({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      color: Colors.deepOrangeAccent[100],
      child: Center(child: Text('สกิลชาเลนเจอร์')),
    );
  }
}
