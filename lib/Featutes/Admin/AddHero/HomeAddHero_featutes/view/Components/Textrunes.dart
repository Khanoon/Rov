import 'package:flutter/material.dart';

class Textrunes extends StatelessWidget {
  const Textrunes({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      color: Colors.deepOrangeAccent[100],
      child: Center(child: Text('รูน')),
    );
  }
}
