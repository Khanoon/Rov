import 'package:flutter/material.dart';

class textHerocombo extends StatelessWidget {
  const textHerocombo({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      color: Colors.deepOrangeAccent[100],
      child: Center(child: Text('ตัวชนะ')),
    );
  }
}
