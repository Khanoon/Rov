import 'package:flutter/material.dart';

class textItem extends StatelessWidget {
  const textItem({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      color: Colors.deepOrangeAccent[100],
      child: Center(child: Text('ไอเทมเเก้')),
    );
  }
}
